import type { Metadata, Viewport } from 'next'

export const metadata: Metadata = {
  title: 'Login | MED1',
  description: 'Área do paciente - Login',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  themeColor: '#1e40af',
} 